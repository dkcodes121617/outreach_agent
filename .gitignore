# Secrets. `.env.example` is the tracked template — never the real file.
.env
.env.*
!.env.example

# Local-only
.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
*.log

# Run output: drafts, assessment dumps.
output/

# Cached clone of the site repo used for grounding.
site_repo/
.cache/

# Prospect data and screenshots are PII. Never in a repo, even a private one.
screenshots/
prospects/
